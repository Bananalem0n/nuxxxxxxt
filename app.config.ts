export default defineAppConfig({
  title: 'Bananalemon Website',
  theme: {
    dark: true,
  },
  nuxtIcon: {
    size: '25px', // default <Icon> size applied
    class: 'icon', // default <Icon> class applied
    aliases: {
      'nuxt': 'logos:nuxt-icon',
    }
  }
})
